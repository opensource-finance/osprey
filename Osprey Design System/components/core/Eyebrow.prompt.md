Tiny UPPERCASE wide-tracked label that introduces a section heading.

```jsx
<Eyebrow tone="info">Active Interception</Eyebrow>
<Eyebrow tone="muted">Engineered by the</Eyebrow>
```

Tones: `muted` (default grey), `accent` (red), `info` (blue). Always pair directly above an `<h2>`.
