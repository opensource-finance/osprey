Base content surface — white, hairline ring, soft 24px corners.

```jsx
<Card>
  <h3>Single Binary</h3>
  <p>Compiled Go. No JVM. No node_modules.</p>
</Card>

<Card interactive>…hover lifts…</Card>
```

`interactive` enables the brand hover (sunken fill, border appears, faint raise) — use for clickable cards. `padding`: `none | sm | md | lg`.
